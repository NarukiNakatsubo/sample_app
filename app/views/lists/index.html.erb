<h1>投稿一覧</h1>

<!--eachメソッドによりListレコードの数だけ繰り返し処理を行う-->
<!--各レコードの内容はlistという変数に格納-->
<% @lists.each do |list| %>

  <p>タイトル</p>
  <!--list.[カラム名]と記述することでレコードのカラムの内容を表示-->
  <span>
    <%= link_to list.title, list_path(list.id) %>
  </span>
  
  <p>画像</p>
  <% if list.image.attached? %>
    <%= image_tag list.image, size: "200x200" %>
  <% else %>
    <%= image_tag 'no_image', size: "200x200" %>
  <% end %>

<% end %>